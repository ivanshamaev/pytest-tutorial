# 📚 Обучающий проект по Pytest

Полнофункциональный пример TODO приложения с расширенным набором тестов, демонстрирующих основные возможности pytest.

## 🚀 Быстрый старт

### Установка зависимостей
```bash
python3 -m venv venv
source venv/bin/activate
pip install pytest
```

> **Примечание:** Файл `pytest.ini` уже настроен для корректного импорта модулей из папки `src/`

### Запуск демо-приложения
```bash
python3 -m src.todo_app
```

### Запуск тестов
```bash
pytest test/ -v
```

## 📋 Структура проекта

```
pytest-tutorial/
├── src/
│   ├── __init__.py
│   └── todo_app.py          # Основное приложение
├── test/
│   ├── conftest.py          # Конфигурация pytest и fixtures
│   └── test_todo_app.py     # Полный набор тестов
├── .gitignore               # Исключения для Git
├── pytest.ini               # Конфигурация pytest
├── README.md                # Этот файл
├── PYTEST_EXAMPLES.md       # Примеры использования
├── PYTEST_PATTERNS.md       # Паттерны тестирования
└── setup_and_run.sh         # Скрипт для быстрого старта
```

## 🎯 Особенности приложения

### Класс Task
- `title`: название задачи
- `done`: статус завершения
- `priority`: приоритет (1-3)
- `created_at`: время создания
- `completed_at`: время завершения

### Класс TodoApp
- `add_task()` - добавить задачу
- `complete_task()` - завершить задачу
- `delete_task()` - удалить задачу
- `get_task()` - получить задачу по названию
- `get_all_tasks()` - получить все задачи
- `active_tasks_count()` - количество активных задач
- `completed_tasks_count()` - количество завершённых задач
- `get_tasks_by_priority()` - фильтрация по приоритету
- `get_high_priority_tasks()` - получить высокоприоритетные задачи

## 📖 Покрытие возможностей pytest

### 1. **Базовые Assertions**
```python
def test_add_task_basic(todo_app):
    task = todo_app.add_task("Learn pytest")
    assert task.title == "Learn pytest"
    assert task.done is False
    assert len(todo_app.get_all_tasks()) == 1
```
- Проверка простых условий
- Проверка булевых значений
- Проверка длины коллекций

### 2. **Тестирование исключений (pytest.raises)**
```python
def test_add_empty_task(todo_app):
    with pytest.raises(ValueError, match="cannot be empty"):
        todo_app.add_task("")
```
- Проверка выброса исключений
- Проверка сообщения об ошибке (match)
- Различные типы исключений

**Примеры тестов:**
- Пустые значения
- Слишком длинные строки
- Неверные значения приоритета
- Несуществующие задачи

### 3. **Fixtures - переиспользуемые данные**
```python
@pytest.fixture
def todo_app(storage_file: Path) -> TodoApp:
    return TodoApp(storage_file)

@pytest.fixture
def app_with_tasks(todo_app: TodoApp) -> TodoApp:
    todo_app.add_task("Task 1", priority=1)
    todo_app.add_task("Task 2", priority=2)
    return todo_app
```
- Создание переиспользуемых объектов для тестов
- Зависимые fixtures (одна fixture использует другую)
- Различные области действия (scope)
- Очистка ресурсов после тестов

### 4. **Параметризация тестов (@pytest.mark.parametrize)**
```python
@pytest.mark.parametrize("title,priority,expected_priority", [
    ("Low priority task", 1, 1),
    ("Medium priority task", 2, 2),
    ("High priority task", 3, 3),
])
def test_add_task_with_different_priorities(
    todo_app, title, priority, expected_priority
):
    task = todo_app.add_task(title, priority=priority)
    assert task.priority == expected_priority
```
- Запуск одного теста с разными параметрами
- Сокращение дублирования кода
- Легко читаемый список тестовых случаев

**Примеры использования:**
- Тестирование разных приоритетов
- Проверка некорректных входных данных
- Граничные случаи

### 5. **Маркеры тестов (@pytest.mark)**
```python
@pytest.mark.critical
def test_persistence_across_instances(todo_app):
    ...

@pytest.mark.slow
def test_many_tasks(todo_app):
    ...

@pytest.mark.integration
def test_complete_workflow(todo_app):
    ...
```
- Категоризация тестов по типам
- Выборочный запуск: `pytest -m "critical"`
- Исключение тестов: `pytest -m "not slow"`

**Доступные маркеры:**
- `critical` - критические тесты
- `slow` - медленные тесты
- `integration` - интеграционные тесты

### 6. **Сложные Assertions**
```python
def test_task_in_list(app_with_tasks):
    tasks = app_with_tasks.get_all_tasks()
    titles = [t.title for t in tasks]
    
    assert "Task 1" in titles
    assert len(titles) == 3
    assert all(t.priority > 0 for t in tasks)
```
- Проверка наличия элемента в списке
- Проверка длины коллекций
- Использование `all()` и `any()`
- Сравнение коллекций

### 7. **Пользовательские Assertions**
```python
def test_all_tasks_have_creation_date(app_with_tasks):
    tasks = app_with_tasks.get_all_tasks()
    
    assert all(task.created_at for task in tasks), \
        "All tasks must have creation timestamp"
```
- Проверка логических условий
- Пользовательские сообщения об ошибках
- Комбинирование условий

### 8. **Тестирование граничных случаев (Edge Cases)**
```python
def test_add_task_with_special_characters(todo_app):
    special_titles = [
        "Task with émojis 🎉",
        "Task with \"quotes\"",
        "Task with\nnewline",
    ]
    
    for title in special_titles:
        task = todo_app.add_task(title)
        assert task.title == title
```
- Специальные символы и Unicode
- Максимальные длины
- Пустые значения
- Граничные значения приоритетов

### 9. **Тестирование состояния и изменений (State Mutations)**
```python
def test_task_count_progression(todo_app):
    assert todo_app.active_tasks_count() == 0
    
    todo_app.add_task("Task 1")
    assert todo_app.active_tasks_count() == 1
    
    todo_app.complete_task("Task 1")
    assert todo_app.active_tasks_count() == 0
```
- Отслеживание изменений состояния
- Проверка побочных эффектов
- Последовательные операции

### 10. **Session-уровневые Fixtures**
```python
@pytest.fixture(scope="session")
def session_info():
    return {
        "test_suite": "TODO App Test Suite",
        "version": "1.0",
    }
```
- Fixtures с областью видимости сессии
- Переиспользование дорогостоящих ресурсов
- Однократная инициализация для всех тестов

### 11. **Классы тестов (Test Classes)**
```python
class TestBasicAssertions:
    def test_add_task_basic(self, todo_app):
        ...
    
    def test_task_equality(self, todo_app):
        ...
```
- Организация тестов в классы
- Группировка связанных тестов
- Улучшенная читаемость

### 12. **pytest.raises с типами исключений**
```python
with pytest.raises(TaskNotFoundError):
    todo_app.complete_task("Nonexistent")
```
- Проверка собственных исключений
- Проверка нескольких типов ошибок
- Использование match для сообщений

## 🔧 Команды pytest

### Базовый запуск
```bash
pytest test/                  # Запуск всех тестов
pytest test/test_todo_app.py  # Запуск конкретного файла
pytest -v                     # Подробный вывод
pytest -q                     # Краткий вывод
```

### Фильтрация по маркерам
```bash
pytest -m "critical"          # Только критические тесты
pytest -m "not slow"          # Все кроме медленных
pytest -m "critical or integration"  # Критические или интеграционные
```

### Запуск конкретных тестов
```bash
pytest test/test_todo_app.py::TestBasicAssertions
pytest test/test_todo_app.py::TestBasicAssertions::test_add_task_basic
```

### Показ информации о тестах
```bash
pytest --collect-only (-co)   # Список всех тестов
pytest --markers              # Доступные маркеры
pytest -h                     # Справка
```

### Дополнительные опции
```bash
pytest -x                     # Остановка на первой ошибке
pytest --maxfail=2            # Остановка после 2 ошибок
pytest --tb=short             # Короткий формат traceback
pytest --tb=no                # Без traceback
pytest -s                     # Показ print() вывода
pytest -k "test_add"          # Запуск тестов по названию
```

## 📊 Статистика проекта

| Метрика | Значение |
|---------|----------|
| Всего тестов | 41 |
| Покрытые возможности pytest | 12+ |
| Классы fixtures | 4 |
| Параметризованные тесты | 8 |
| Тесты с маркерами | 9 |

## 🎓 Обучающие моменты

### Почему эта структура полезна?

1. **Реалистичное приложение** - TODO app имеет смысл в реальной жизни
2. **Полное покрытие** - демонстрирует все основные возможности pytest
3. **Хорошо организовано** - код разделен на логические секции
4. **Легко модифицировать** - просто добавить новые функции и тесты
5. **Образец для проектов** - можно использовать как шаблон

### Что выучится разработчик?

✅ Как писать хорошие unit-тесты  
✅ Как использовать fixtures  
✅ Как параметризировать тесты  
✅ Как тестировать исключения  
✅ Как организовать тесты в классы  
✅ Как использовать маркеры для фильтрации  
✅ Как тестировать сложное состояние приложения  
✅ Как работать с temporary files  
✅ Лучшие практики в написании тестов  

## 🚀 Возможные расширения

1. **Coverage отчёты**
   ```bash
   pip install pytest-cov
   pytest --cov=src test/
   ```

2. **Performance тестирование**
   ```bash
   pip install pytest-benchmark
   ```

3. **Моки и patching**
   ```bash
   pip install pytest-mock
   ```

4. **Асинхронное тестирование**
   ```bash
   pip install pytest-asyncio
   ```

5. **HTML отчёты**
   ```bash
   pip install pytest-html
   pytest --html=report.html
   ```

## 📝 Лицензия

MIT

---

Сделано как обучающий пример 📚
